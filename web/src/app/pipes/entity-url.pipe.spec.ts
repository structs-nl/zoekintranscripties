import { EntityUrlPipe } from './entity-url.pipe';

describe('EntityUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
