import { CalculateHeightPipe } from './calculate-height.pipe';

describe('CalculateHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
