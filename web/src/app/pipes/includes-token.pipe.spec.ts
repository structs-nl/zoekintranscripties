import { IncludesTokenPipe } from './includes-token.pipe';

describe('IncludesTokenPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludesTokenPipe();
    expect(pipe).toBeTruthy();
  });
});
